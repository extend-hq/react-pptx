# @extend-ai/react-pptx-model

Format-neutral presentation types shared by the Rust parser, worker protocol, and React viewer.
Coordinates are expressed in intrinsic presentation units unless a field explicitly names EMUs.
