# @extend-ai/react-pptx-wasm

Lazy Rust/WebAssembly parser runtime used by `@extend-ai/react-pptx`. Most consumers should
install the React package instead. The runtime exports initialization, format detection, and
normalized presentation parsing, and supports custom Wasm sources for CDN/self-hosted deployments.
