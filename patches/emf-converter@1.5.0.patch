diff --git a/dist/index.js b/dist/index.js
index 6a581a039cfedce9c51207812a13ffa799dd098a..d51f28668d3850393bf3163955630070cf768bd0 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -326,19 +326,19 @@ function mapFontFamily(face, map) {
   }
   return resolved;
 }
-function applyFont(ctx, state) {
+function applyFont(ctx, state, fontHeight = state.fontHeight) {
   const italic = state.fontItalic ? "italic " : "";
   const weight = cssFontWeight(state.fontWeight);
   const weightPart = weight ? `${weight} ` : "";
-  const size = Math.max(Math.abs(state.fontHeight), 8);
+  const size = Math.max(Math.abs(fontHeight), 8);
   const family = mapFontFamily(state.fontFamily, state.fontFamilyMap);
   ctx.font = `${italic}${weightPart}${size}px ${family}`;
 }
-function drawTextDecorations(ctx, state, x, y, width) {
+function drawTextDecorations(ctx, state, x, y, width, fontHeight = state.fontHeight) {
   if (!state.fontUnderline && !state.fontStrikeOut) {
     return;
   }
-  const size = Math.max(Math.abs(state.fontHeight), 8);
+  const size = Math.max(Math.abs(fontHeight), 8);
   const thickness = Math.max(1, Math.round(size / 14));
   const prevFill = ctx.fillStyle;
   ctx.fillStyle = state.textColor;
@@ -560,25 +560,27 @@ function argbToRgba(argb) {
 // src/emf-gdi-coord.ts
 function gmx(r, x) {
   if (r.useMappingMode) {
-    return (x - r.windowOrg.x) / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) + r.viewportOrg.x;
+    const deviceX = (x - r.windowOrg.x) / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) + r.viewportOrg.x;
+    return (deviceX - r.bounds.left) * r.sx;
   }
   return (x - r.bounds.left) * r.sx;
 }
 function gmy(r, y) {
   if (r.useMappingMode) {
-    return (y - r.windowOrg.y) / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) + r.viewportOrg.y;
+    const deviceY = (y - r.windowOrg.y) / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) + r.viewportOrg.y;
+    return (deviceY - r.bounds.top) * r.sy;
   }
   return (y - r.bounds.top) * r.sy;
 }
 function gmw(r, w) {
   if (r.useMappingMode) {
-    return w / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1);
+    return w / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) * r.sx;
   }
   return w * r.sx;
 }
 function gmh(r, h) {
   if (r.useMappingMode) {
-    return h / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1);
+    return h / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) * r.sy;
   }
   return h * r.sy;
 }
@@ -1104,7 +1106,8 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
     if (nChars > 0 && offString > 0 && offset + offString + nChars * 2 <= maxOffset) {
       const text = readUtf16LE(view, offset + offString, nChars);
       if (text.length > 0) {
-        applyFont(ctx, state);
+        const fontHeight = Math.abs(gmh(rCtx, state.fontHeight));
+        applyFont(ctx, state, fontHeight);
         ctx.fillStyle = state.textColor;
         let alignBaseline = "alphabetic";
         let alignHoriz = "left";
@@ -1124,7 +1127,7 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
         ctx.textAlign = alignHoriz;
         if (state.bkMode === 2) {
           const measured = ctx.measureText(text);
-          const bgH = state.fontHeight || 12;
+          const bgH = fontHeight || 12;
           ctx.fillStyle = state.bkColor;
           ctx.fillRect(gmx(rCtx, refX), gmy(rCtx, refY) - bgH, measured.width, bgH);
           ctx.fillStyle = state.textColor;
@@ -1134,7 +1137,7 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
           const w = ctx.measureText(text).width;
           const baseX = gmx(rCtx, refX);
           const startX = alignHoriz === "center" ? baseX - w / 2 : alignHoriz === "right" ? baseX - w : baseX;
-          drawTextDecorations(ctx, state, startX, gmy(rCtx, refY), w);
+          drawTextDecorations(ctx, state, startX, gmy(rCtx, refY), w, fontHeight);
         }
       }
     }
@@ -1685,7 +1688,7 @@ function handleEmfObjectRecord(rCtx, recType, dataOff, recSize) {
         const italic = view.getUint8(dataOff + 24);
         const underline = view.getUint8(dataOff + 25);
         const strikeOut = view.getUint8(dataOff + 26);
-        const family = readUtf16LE(view, dataOff + 28, 32) || "sans-serif";
+        const family = readUtf16LE(view, dataOff + 32, 32) || "sans-serif";
         rCtx.objectTable.set(ihFont, {
           kind: "font",
           height: Math.abs(height),
@@ -3623,7 +3626,7 @@ function replayEmfRecords(view, ctx, bounds, canvasW, canvasH, dpiScale = 1, rep
     windowOrg: { x: bounds.left, y: bounds.top },
     windowExt: { cx: logicalW, cy: logicalH },
     viewportOrg: { x: 0, y: 0 },
-    viewportExt: { cx: canvasW, cy: canvasH },
+    viewportExt: { cx: logicalW, cy: logicalH },
     useMappingMode: false,
     clipSaveDepth: 0,
     bounds,
diff --git a/dist/index.mjs b/dist/index.mjs
index 9c71f8b2b8d197fb14ab9efea2f6160e821a327a..90237b70107bf1e54fab479a113b9197a13524c0 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -324,19 +324,19 @@ function mapFontFamily(face, map) {
   }
   return resolved;
 }
-function applyFont(ctx, state) {
+function applyFont(ctx, state, fontHeight = state.fontHeight) {
   const italic = state.fontItalic ? "italic " : "";
   const weight = cssFontWeight(state.fontWeight);
   const weightPart = weight ? `${weight} ` : "";
-  const size = Math.max(Math.abs(state.fontHeight), 8);
+  const size = Math.max(Math.abs(fontHeight), 8);
   const family = mapFontFamily(state.fontFamily, state.fontFamilyMap);
   ctx.font = `${italic}${weightPart}${size}px ${family}`;
 }
-function drawTextDecorations(ctx, state, x, y, width) {
+function drawTextDecorations(ctx, state, x, y, width, fontHeight = state.fontHeight) {
   if (!state.fontUnderline && !state.fontStrikeOut) {
     return;
   }
-  const size = Math.max(Math.abs(state.fontHeight), 8);
+  const size = Math.max(Math.abs(fontHeight), 8);
   const thickness = Math.max(1, Math.round(size / 14));
   const prevFill = ctx.fillStyle;
   ctx.fillStyle = state.textColor;
@@ -558,25 +558,27 @@ function argbToRgba(argb) {
 // src/emf-gdi-coord.ts
 function gmx(r, x) {
   if (r.useMappingMode) {
-    return (x - r.windowOrg.x) / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) + r.viewportOrg.x;
+    const deviceX = (x - r.windowOrg.x) / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) + r.viewportOrg.x;
+    return (deviceX - r.bounds.left) * r.sx;
   }
   return (x - r.bounds.left) * r.sx;
 }
 function gmy(r, y) {
   if (r.useMappingMode) {
-    return (y - r.windowOrg.y) / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) + r.viewportOrg.y;
+    const deviceY = (y - r.windowOrg.y) / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) + r.viewportOrg.y;
+    return (deviceY - r.bounds.top) * r.sy;
   }
   return (y - r.bounds.top) * r.sy;
 }
 function gmw(r, w) {
   if (r.useMappingMode) {
-    return w / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1);
+    return w / (r.windowExt.cx || 1) * (r.viewportExt.cx || 1) * r.sx;
   }
   return w * r.sx;
 }
 function gmh(r, h) {
   if (r.useMappingMode) {
-    return h / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1);
+    return h / (r.windowExt.cy || 1) * (r.viewportExt.cy || 1) * r.sy;
   }
   return h * r.sy;
 }
@@ -1102,7 +1104,8 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
     if (nChars > 0 && offString > 0 && offset + offString + nChars * 2 <= maxOffset) {
       const text = readUtf16LE(view, offset + offString, nChars);
       if (text.length > 0) {
-        applyFont(ctx, state);
+        const fontHeight = Math.abs(gmh(rCtx, state.fontHeight));
+        applyFont(ctx, state, fontHeight);
         ctx.fillStyle = state.textColor;
         let alignBaseline = "alphabetic";
         let alignHoriz = "left";
@@ -1122,7 +1125,7 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
         ctx.textAlign = alignHoriz;
         if (state.bkMode === 2) {
           const measured = ctx.measureText(text);
-          const bgH = state.fontHeight || 12;
+          const bgH = fontHeight || 12;
           ctx.fillStyle = state.bkColor;
           ctx.fillRect(gmx(rCtx, refX), gmy(rCtx, refY) - bgH, measured.width, bgH);
           ctx.fillStyle = state.textColor;
@@ -1132,7 +1135,7 @@ function handleExtTextOutW(rCtx, offset, dataOff, recSize) {
           const w = ctx.measureText(text).width;
           const baseX = gmx(rCtx, refX);
           const startX = alignHoriz === "center" ? baseX - w / 2 : alignHoriz === "right" ? baseX - w : baseX;
-          drawTextDecorations(ctx, state, startX, gmy(rCtx, refY), w);
+          drawTextDecorations(ctx, state, startX, gmy(rCtx, refY), w, fontHeight);
         }
       }
     }
@@ -1683,7 +1686,7 @@ function handleEmfObjectRecord(rCtx, recType, dataOff, recSize) {
         const italic = view.getUint8(dataOff + 24);
         const underline = view.getUint8(dataOff + 25);
         const strikeOut = view.getUint8(dataOff + 26);
-        const family = readUtf16LE(view, dataOff + 28, 32) || "sans-serif";
+        const family = readUtf16LE(view, dataOff + 32, 32) || "sans-serif";
         rCtx.objectTable.set(ihFont, {
           kind: "font",
           height: Math.abs(height),
@@ -3621,7 +3624,7 @@ function replayEmfRecords(view, ctx, bounds, canvasW, canvasH, dpiScale = 1, rep
     windowOrg: { x: bounds.left, y: bounds.top },
     windowExt: { cx: logicalW, cy: logicalH },
     viewportOrg: { x: 0, y: 0 },
-    viewportExt: { cx: canvasW, cy: canvasH },
+    viewportExt: { cx: logicalW, cy: logicalH },
     useMappingMode: false,
     clipSaveDepth: 0,
     bounds,
